<?php
/**
 * Functions file
 *  
 * (c) 2025, Andrei S. Osintsev
*/

function createURLHash($urlString = ''): string {
    if (!$urlString) return '';
    // return md5($urlString);
}

function getURLbyHash($hash = ''): string {
    if (!$hash) return '';
}